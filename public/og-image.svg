<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#1e293b"/>

  <!-- Gradient overlay -->
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1e293b;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#334155;stop-opacity:1" />
    </linearGradient>
  </defs>
  <rect width="1200" height="630" fill="url(#grad)"/>

  <!-- Main content -->
  <g transform="translate(600, 315)">
    <!-- Dog emoji as main icon -->
    <text x="0" y="-80" font-size="120" text-anchor="middle">🐕</text>

    <!-- App name -->
    <text x="0" y="40" font-family="Arial, sans-serif" font-size="72" font-weight="bold" fill="#f8fafc" text-anchor="middle">Motekita</text>

    <!-- Tagline in Japanese -->
    <text x="0" y="100" font-family="Arial, sans-serif" font-size="32" fill="#cbd5e1" text-anchor="middle">メールが届くと、犬が教えてくれる</text>

    <!-- Mail icon -->
    <text x="-280" y="-80" font-size="60" opacity="0.3">📧</text>
    <text x="280" y="-80" font-size="60" opacity="0.3">📧</text>
  </g>

  <!-- Footer info -->
  <text x="600" y="580" font-family="Arial, sans-serif" font-size="24" fill="#94a3b8" text-anchor="middle">Windows • macOS • Linux</text>
</svg>
