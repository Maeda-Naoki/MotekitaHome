---
interface Props {
  title: string;
  description: string;
  image?: string;
  type?: 'website' | 'article';
  jsonLd?: Record<string, unknown>;
  keywords?: string;
}

const { title, description, image = '/og-image.svg', type = 'website', jsonLd, keywords } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const lang = Astro.currentLocale || 'ja';

// デフォルトのキーワード
const defaultKeywords = lang === 'ja'
  ? 'Motekita,モテきた,モテキタ,もてきた,メール通知,デスクトップアプリ,メールアプリ,ゴールデンレトリバー,通知アプリ,プライバシー重視,ローカル処理,Windows,macOS,Linux'
  : 'Motekita,email notification,desktop app,email app,Golden Retriever,notification app,privacy focused,local processing,Windows,macOS,Linux';

const finalKeywords = keywords || defaultKeywords;

const jaUrl = new URL(Astro.url.pathname.replace(/^\/(ja|en)/, '/ja'), Astro.site);
const enUrl = new URL(Astro.url.pathname.replace(/^\/(ja|en)/, '/en'), Astro.site);

const organizationJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'Motekita',
  url: Astro.site?.toString(),
  logo: new URL('/favicon.png', Astro.site).toString(),
};

const webSiteJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'Motekita',
  url: Astro.site?.toString(),
  inLanguage: lang === 'ja' ? 'ja-JP' : 'en-US',
};
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>{title}</title>
<meta name="description" content={description} />
<meta name="keywords" content={finalKeywords} />
<meta name="google-site-verification" content="SJAdcON3OdoEDs_CTmMU26mekhl2v3PGLSgQL0sTq-Q" />
<link rel="canonical" href={canonicalURL} />

<!-- Additional SEO meta -->
<meta name="robots" content="index, follow" />
<meta name="author" content="Motekita" />
<meta name="generator" content={Astro.generator} />

<!-- hreflang -->
<link rel="alternate" hreflang="ja" href={jaUrl} />
<link rel="alternate" hreflang="en" href={enUrl} />
<link rel="alternate" hreflang="x-default" href={jaUrl} />

<!-- Open Graph -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.site)} />
<meta property="og:locale" content={lang === 'ja' ? 'ja_JP' : 'en_US'} />
<meta property="og:locale:alternate" content={lang === 'ja' ? 'en_US' : 'ja_JP'} />
<meta property="og:site_name" content="Motekita" />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={new URL(image, Astro.site)} />

<!-- Favicon -->
<link rel="icon" type="image/png" sizes="32x32" href="/favicon.png" />
<link rel="icon" type="image/png" sizes="16x16" href="/favicon.png" />
<link rel="apple-touch-icon" sizes="180x180" href="/favicon.png" />
<link rel="shortcut icon" href="/favicon.png" />

<!-- Theme Color -->
<meta name="theme-color" content="#1e293b" />
<meta name="theme-color" content="#f8fafc" media="(prefers-color-scheme: light)" />

<!-- JSON-LD Structured Data -->
<script type="application/ld+json" set:html={JSON.stringify(organizationJsonLd)} />
<script type="application/ld+json" set:html={JSON.stringify(webSiteJsonLd)} />
{jsonLd && (
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
)}
